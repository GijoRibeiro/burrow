# Requested next

- **Project servers:** Show running development servers associated with each
  project or worktree, with clickable addresses such as `localhost:3000` or
  `app.cloover`. Include custom hostnames when available, and update the list
  when servers start or stop. Detect servers already running, including those
  started outside the app.


Server visibility remains queued. The active-project filter shipped with the head-agent canvas on September 16, 2026.
